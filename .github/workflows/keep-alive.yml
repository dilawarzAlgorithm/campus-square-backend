name: Keep Render Server Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Space
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://campus-square-backend.onrender.com/docs
